#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for i in matrix:
        for j in i):
            if k is not i[len(matrix) - 1]:
                print("{:d}".format(k), end=" ")
            else:
                print("{:d}".format(k), end="")
        print()
